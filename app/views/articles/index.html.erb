<section class="Blog">
  <div class="container">
    <h1>Blog</h1>
    <% @articles.each do |article| %>
      <h3>
      <%= link_to article.title, article_path(article) %>
      </h3>
      <%= truncate(article.body, length:100) %>
    <% end %>
  </div>
</div>
  